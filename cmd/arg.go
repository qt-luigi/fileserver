package main

const (
	usageMsg = `fileserver is a static file HTTP server.

Usage:
	fileserver [-p port] [-d directory]

Arguments are:

	-d directory
		file directory (default "./")
	-p port
		port number (default 8080)
`
)
